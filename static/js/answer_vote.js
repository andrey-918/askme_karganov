const init() = () => {    const answers = document.querySelector('.answer_card')    for (const answer of answers) {        const likeButton = answer.querySelector('.like-button')        const likeCounter = answer.querySelector('.like-counter')        likeButton.addEventListener('click', () => {            likeCounter.innerHTML = (Number(likeCounter.innerHTML) + 1).toString()        })    }}init()